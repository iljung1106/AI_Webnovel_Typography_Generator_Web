# Shared Contracts

Shared TypeScript contracts used by the Next.js frontend.

The Python API has its own Pydantic schemas for now. Keep the contracts aligned manually until an OpenAPI-based generation step is introduced.
